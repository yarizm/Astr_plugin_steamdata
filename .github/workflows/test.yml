name: Test

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r astrbot_plugin_steamdata/requirements.txt
          python -m pip install ruff

      - name: Compile plugin entry
        run: python -m py_compile astrbot_plugin_steamdata/main.py

      - name: Run unit tests
        run: python -m unittest discover -s tests -v

      - name: Run ruff
        run: ruff check .
